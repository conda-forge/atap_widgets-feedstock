{% set name = "atap_widgets" %}
{% set version = "0.2.21" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/atap_widgets-{{ version }}.tar.gz
  sha256: dab22cb7287c937ee8464faa7d08f664662dbadcb4150cb2ad877e12a34297cb

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - poetry
    - python >=3.6,<3.11
  run:
    - bokeh >=2.4,<3.0
    - ipywidgets >=7.6,<8.0
    - pandas >=1.3,<2.0
    - python >=3.6,<3.11
    - sentence-transformers >=2.1.0,<3.0.0
    - spacy >=3.1,<4.0
    - textacy
    - xlsxwriter >=3.0.2,<4.0.0

test:
  imports:
    - atap_widgets
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Australian-Text-Analytics-Platform/atap_widgets
  summary: Interactive widgets used by the Australian Text Analytics Platform
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - marius-mather
